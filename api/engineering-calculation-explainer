export default async function handler(req, res) {
  return res.status(200).json({
    ok: true,
    output: "ENGINEERING ENDPOINT CONFIRMED\n• If you see this, JSON rendering works.\n• Streaming is bypassed."
  });
}
