export default async function handler(req, res) {
  res.setHeader("Content-Type", "application/json");

  return res.status(200).json({
    ok: true,
    output:
      "ENGINEERING BACKEND CONFIRMED\n" +
      "• This endpoint returns JSON\n" +
      "• Streaming is disabled"
  });
}
